# Зависимости крипто-советника (см. app/).
# Всё асинхронное: ccxt.async_support + python-telegram-bot 22.

# --- Данные бирж ------------------------------------------------------------
# ccxt включает ccxt.pro (WebSocket) и async_support (REST).
ccxt>=4.5.0,<5.0
httpx>=0.27.0,<1.0

# --- Telegram ---------------------------------------------------------------
python-telegram-bot>=22.0,<23.0

# --- Вычисления -------------------------------------------------------------
# Индикаторы написаны сами (см. app/indicators) на pandas/numpy:
# библиотека ta-lib/ta не тянется, чтобы не зависеть от C-расширений
# и от несовместимости ta с pandas 3.x.
pandas>=2.2,<4.0
numpy>=1.26,<3.0

# --- Конфигурация -----------------------------------------------------------
python-dotenv>=1.0.0,<2.0
